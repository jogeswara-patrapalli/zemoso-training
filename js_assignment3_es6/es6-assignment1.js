// Refactor the following function into a one-liner:
// const printName = (name) => {
//                      return “Hi” + name;
//            }

//using template string literals
const printName = (name) => `Hi ${name}`;
console.log(printName('Jogi'));







  
